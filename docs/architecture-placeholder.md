# Architecture

Architecture definition pending user review. The future source layout must keep game data, layouts, widgets, Routines, and assets separate and generate VirtualTabletop-compatible JSON rather than directly maintaining a monolithic `0.json`.

